name: ⏰ Keep Render Bot Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # Alle 5 Minuten
  workflow_dispatch:        # Optional: manuelles Auslösen über GitHub

jobs:
  ping-render:
    runs-on: ubuntu-latest

    steps:
      - name: 📡 Ping POST to Render
        run: |
          curl -X POST https://matchingflobot.onrender.com/webhook -H "Content-Type: application/json" -d '{}'
