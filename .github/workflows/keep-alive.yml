name: ⏰ Keep Render Bot Alive

on:
  schedule:
    - cron: '*/5 * * * *'    # Alle 5 Minuten ausführen
  workflow_dispatch:         # Manuelles Starten weiterhin möglich

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: 🔁 Ping Render App
        run: curl -s https://matchingflobot.onrender.com/ > /dev/null
